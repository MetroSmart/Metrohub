[pytest]
pythonpath = .
testpaths = tests
addopts = -ra --strict-markers
markers =
    postgres: requiere PostgreSQL real (vistas/triggers no soportados en SQLite)
    slow: tests lentos
filterwarnings =
    ignore::DeprecationWarning
